import { useState } from 'react';

const FindPeople = () => {
    const [users, setUsers] = useState([]);
    return (
        <div>
            <p>Find People</p>
        </div>
    );
};

export default FindPeople;
